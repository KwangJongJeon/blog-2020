import React, { Component } from 'react';

class Menulist extends Component {
    render() {
        let className = '';
        const {isActive} = this.props;

        if(isActive) {
            className += 'is-active'
        }

        return 
    }
}